<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Focus Fx Auto V75 TRADERS</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=DM+Mono:ital,wght@0,300;0,400;0,500;1,300&family=Syne:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js"></script>
<style>
  :root {
    --bg: #080b0f;
    --bg2: #0d1117;
    --bg3: #121920;
    --bg4: #1a2332;
    --border: rgba(99, 210, 160, 0.12);
    --border2: rgba(99, 210, 160, 0.25);
    --accent: #63d2a0;
    --accent2: #3ab07a;
    --accent-dim: rgba(99, 210, 160, 0.08);
    --text: #e8f0ea;
    --text2: #7a9d8a;
    --text3: #445a50;
    --red: #ff6b6b;
    --amber: #f0a94a;
    --blue: #5ba3e0;
    --mono: 'DM Mono', monospace;
    --display: 'Syne', sans-serif;
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html { scroll-behavior: smooth; }

  body {
    font-family: var(--mono);
    background: var(--bg);
    color: var(--text);
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* Grid background */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image:
      linear-gradient(rgba(99,210,160,0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(99,210,160,0.03) 1px, transparent 1px);
    background-size: 40px 40px;
    pointer-events: none;
    z-index: 0;
  }

  /* Glow orb */
  body::after {
    content: '';
    position: fixed;
    top: -200px;
    right: -200px;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(99,210,160,0.06) 0%, transparent 70%);
    pointer-events: none;
    z-index: 0;
  }

  .wrapper {
    position: relative;
    z-index: 1;
    max-width: 860px;
    margin: 0 auto;
    padding: 40px 24px 80px;
  }

  /* ── Header ── */
  .header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    margin-bottom: 48px;
    animation: fadeUp 0.5s ease both;
  }

  .brand {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .logo-row {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .logo-icon {
    width: 36px;
    height: 36px;
    border: 1px solid var(--accent);
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
  }

  .logo-icon::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, var(--accent-dim), transparent);
  }

  .logo-icon svg { width: 20px; height: 20px; stroke: var(--accent); }

  h1 {
    font-family: var(--display);
    font-size: 22px;
    font-weight: 700;
    letter-spacing: -0.5px;
    color: var(--text);
  }

  .tagline {
    font-size: 12px;
    color: var(--text2);
    letter-spacing: 0.5px;
  }

  .groq-badge {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 6px 12px;
    border: 1px solid var(--border2);
    border-radius: 20px;
    font-size: 11px;
    color: var(--accent);
    background: var(--accent-dim);
    letter-spacing: 0.5px;
  }

  .groq-badge::before {
    content: '';
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--accent);
    animation: blink 2s ease infinite;
  }

  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:0.3} }

  /* ── Panels ── */
  .panel {
    background: var(--bg2);
    border: 1px solid var(--border);
    border-radius: 12px;
    overflow: hidden;
    margin-bottom: 16px;
    animation: fadeUp 0.5s ease both;
  }

  .panel-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 12px 18px;
    border-bottom: 1px solid var(--border);
    background: var(--bg3);
  }

  .panel-title {
    font-size: 11px;
    font-weight: 500;
    color: var(--text2);
    letter-spacing: 1.5px;
    text-transform: uppercase;
  }

  .panel-body { padding: 20px 18px; }

  /* ── API Key ── */
  .key-row { display: flex; gap: 10px; }

  .key-input {
    flex: 1;
    padding: 10px 14px;
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 8px;
    font-family: var(--mono);
    font-size: 13px;
    color: var(--accent);
    outline: none;
    transition: border-color 0.2s;
    letter-spacing: 0.5px;
  }

  .key-input::placeholder { color: var(--text3); }
  .key-input:focus { border-color: var(--border2); }

  .key-link {
    padding: 10px 14px;
    border: 1px solid var(--border2);
    border-radius: 8px;
    font-size: 11px;
    color: var(--accent);
    text-decoration: none;
    white-space: nowrap;
    display: flex;
    align-items: center;
    gap: 6px;
    transition: all 0.2s;
    background: var(--accent-dim);
  }

  .key-link:hover { background: rgba(99,210,160,0.15); }

  /* ── Tabs ── */
  .tabs {
    display: flex;
    gap: 0;
    border: 1px solid var(--border);
    border-radius: 8px;
    overflow: hidden;
    background: var(--bg);
  }

  .tab-btn {
    flex: 1;
    padding: 9px 14px;
    font-family: var(--mono);
    font-size: 12px;
    border: none;
    background: transparent;
    color: var(--text2);
    cursor: pointer;
    transition: all 0.15s;
    border-right: 1px solid var(--border);
    letter-spacing: 0.3px;
  }

  .tab-btn:last-child { border-right: none; }

  .tab-btn.active {
    background: var(--accent-dim);
    color: var(--accent);
  }

  .tab-btn:hover:not(.active) { color: var(--text); background: rgba(255,255,255,0.02); }

  /* ── Upload zone ── */
  .upload-zone {
    border: 1.5px dashed var(--border2);
    border-radius: 10px;
    padding: 40px 24px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    cursor: pointer;
    transition: all 0.2s;
    background: var(--bg);
    position: relative;
    overflow: hidden;
  }

  .upload-zone::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at center bottom, rgba(99,210,160,0.04) 0%, transparent 70%);
    pointer-events: none;
  }

  .upload-zone:hover, .upload-zone.drag {
    border-color: var(--accent);
    background: var(--accent-dim);
  }

  .upload-icon svg { width: 40px; height: 40px; stroke: var(--text3); transition: stroke 0.2s; }
  .upload-zone:hover .upload-icon svg, .upload-zone.drag .upload-icon svg { stroke: var(--accent); }

  .upload-title { font-family: var(--display); font-size: 16px; font-weight: 600; color: var(--text); }
  .upload-sub { font-size: 12px; color: var(--text2); }

  .img-preview-wrap { margin-top: 14px; }
  .img-preview {
    width: 100%;
    max-height: 220px;
    object-fit: contain;
    border-radius: 8px;
    border: 1px solid var(--border);
    background: var(--bg);
    padding: 8px;
  }

  .remove-btn {
    margin-top: 8px;
    font-family: var(--mono);
    font-size: 11px;
    padding: 5px 12px;
    border: 1px solid var(--border);
    border-radius: 6px;
    background: transparent;
    color: var(--text2);
    cursor: pointer;
    transition: all 0.15s;
  }

  .remove-btn:hover { border-color: var(--red); color: var(--red); }

  /* ── Chart builder ── */
  .field-group { display: flex; flex-direction: column; gap: 5px; margin-bottom: 14px; }
  .field-label { font-size: 11px; color: var(--text2); letter-spacing: 0.8px; text-transform: uppercase; }

  .field-input, .field-select {
    padding: 9px 12px;
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 7px;
    font-family: var(--mono);
    font-size: 13px;
    color: var(--text);
    outline: none;
    transition: border-color 0.2s;
    width: 100%;
  }

  .field-input::placeholder { color: var(--text3); }
  .field-input:focus, .field-select:focus { border-color: var(--border2); }
  .field-select option { background: var(--bg2); }

  .grid2 { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }

  .chart-preview-box {
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 16px;
    margin-top: 4px;
  }

  .chart-preview-box canvas { max-height: 200px; }

  /* ── Text area ── */
  .field-textarea {
    width: 100%;
    padding: 12px;
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 8px;
    font-family: var(--mono);
    font-size: 13px;
    color: var(--text);
    outline: none;
    resize: vertical;
    min-height: 100px;
    line-height: 1.6;
    transition: border-color 0.2s;
  }

  .field-textarea::placeholder { color: var(--text3); }
  .field-textarea:focus { border-color: var(--border2); }

  /* ── Prompt chips ── */
  .chips { display: flex; flex-wrap: wrap; gap: 6px; }

  .chip {
    padding: 5px 12px;
    border: 1px solid var(--border);
    border-radius: 20px;
    font-size: 11px;
    font-family: var(--mono);
    color: var(--text2);
    cursor: pointer;
    background: var(--bg);
    transition: all 0.15s;
    letter-spacing: 0.2px;
  }

  .chip:hover { border-color: var(--border2); color: var(--accent); background: var(--accent-dim); }
  .chip.selected { border-color: var(--accent); color: var(--accent); background: var(--accent-dim); }

  .custom-input {
    margin-top: 10px;
    width: 100%;
    padding: 9px 12px;
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 8px;
    font-family: var(--mono);
    font-size: 13px;
    color: var(--text);
    outline: none;
    transition: border-color 0.2s;
  }

  .custom-input::placeholder { color: var(--text3); }
  .custom-input:focus { border-color: var(--border2); }

  /* ── Analyse button ── */
  .analyse-btn {
    width: 100%;
    padding: 14px;
    background: var(--accent);
    border: none;
    border-radius: 10px;
    font-family: var(--display);
    font-size: 15px;
    font-weight: 700;
    color: #07120e;
    cursor: pointer;
    letter-spacing: 0.3px;
    transition: all 0.2s;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    position: relative;
    overflow: hidden;
  }

  .analyse-btn::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.15), transparent);
    transform: translateX(-100%);
    transition: transform 0.5s;
  }

  .analyse-btn:hover::before { transform: translateX(100%); }
  .analyse-btn:hover { background: #78e5b4; transform: translateY(-1px); }
  .analyse-btn:active { transform: translateY(0); }
  .analyse-btn:disabled { opacity: 0.4; cursor: not-allowed; transform: none; }
  .analyse-btn:disabled::before { display: none; }

  .btn-icon svg { width: 18px; height: 18px; stroke: #07120e; }

  /* ── Result ── */
  .result-panel {
    background: var(--bg2);
    border: 1px solid var(--border2);
    border-radius: 12px;
    overflow: hidden;
    display: none;
    animation: fadeUp 0.3s ease;
  }

  .result-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 12px 18px;
    background: var(--bg3);
    border-bottom: 1px solid var(--border);
  }

  .result-head-left { display: flex; align-items: center; gap: 10px; }

  .result-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--accent);
    animation: blink 1.5s ease infinite;
  }

  .result-title { font-size: 11px; color: var(--accent); font-weight: 500; letter-spacing: 1.5px; text-transform: uppercase; }

  .model-tag {
    font-size: 10px;
    padding: 3px 8px;
    border: 1px solid var(--border);
    border-radius: 12px;
    color: var(--text2);
    background: var(--bg);
    font-family: var(--mono);
  }

  .result-body {
    padding: 20px 18px;
    font-size: 13px;
    line-height: 1.8;
    color: var(--text);
    white-space: pre-wrap;
    font-family: var(--mono);
  }

  /* Highlight data-like patterns */
  .result-body span.num { color: var(--accent); }

  /* ── Error ── */
  .error-box {
    display: none;
    padding: 10px 14px;
    background: rgba(255,107,107,0.08);
    border: 1px solid rgba(255,107,107,0.25);
    border-radius: 8px;
    font-size: 12px;
    color: var(--red);
    font-family: var(--mono);
  }

  /* ── Typing indicator ── */
  .typing { display: flex; gap: 5px; align-items: center; padding: 4px 0; }
  .typing span {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: var(--accent);
    animation: pulse 1.2s ease infinite;
  }
  .typing span:nth-child(2) { animation-delay: 0.2s; }
  .typing span:nth-child(3) { animation-delay: 0.4s; }
  @keyframes pulse { 0%,100%{opacity:0.2;transform:scale(0.8)} 50%{opacity:1;transform:scale(1)} }

  /* ── Divider ── */
  .divider {
    height: 1px;
    background: var(--border);
    margin: 4px 0;
  }

  /* ── Animations ── */
  @keyframes fadeUp {
    from { opacity:0; transform: translateY(12px); }
    to   { opacity:1; transform: translateY(0); }
  }

  .panel:nth-child(1) { animation-delay: 0.05s; }
  .panel:nth-child(2) { animation-delay: 0.1s; }
  .panel:nth-child(3) { animation-delay: 0.15s; }
  .panel:nth-child(4) { animation-delay: 0.2s; }
  .panel:nth-child(5) { animation-delay: 0.25s; }

  /* ── Tab content ── */
  .tab-content { display: none; }
  .tab-content.active { display: block; }

  /* Scrollbar */
  ::-webkit-scrollbar { width: 6px; }
  ::-webkit-scrollbar-track { background: var(--bg); }
  ::-webkit-scrollbar-thumb { background: var(--bg4); border-radius: 3px; }
  ::-webkit-scrollbar-thumb:hover { background: var(--text3); }

  /* Footer */
  .footer {
    text-align: center;
    margin-top: 48px;
    font-size: 11px;
    color: var(--text3);
    letter-spacing: 0.5px;
  }

  .footer a { color: var(--text2); text-decoration: none; }
  .footer a:hover { color: var(--accent); }
</style>
</head>
<body>
<div class="wrapper">

  <!-- Header -->
  <div class="header">
    <div class="brand">
      <div class="logo-row">
        <div class="logo-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>
          </svg>
        </div>
        <h1>Focus Fx Auto V75 TRADERS</h1>
      </div>
      <div class="tagline">// V75 AI-powered forex signal intelligence</div>
    </div>
    <div class="groq-badge">V75 · GROQ POWERED</div>
  </div>

  <!-- API Key -->
  <div class="panel" style="animation-delay:0.05s">
    <div class="panel-head">
      <span class="panel-title">Authentication</span>
      <span style="font-size:10px;color:var(--text3)">encrypted in-session</span>
    </div>
    <div class="panel-body">
      <div class="key-row">
        <input type="password" class="key-input" id="apiKey" placeholder="gsk_••••••••••••••••••••••••••••••••••••••••••••••••••••••" />
        <a href="https://console.groq.com" target="_blank" class="key-link">
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 13v6a2 2 0 01-2 2H5a2 2 0 01-2-2V8a2 2 0 012-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
          Get key
        </a>
      </div>
    </div>
  </div>

  <!-- Input method -->
  <div class="panel" style="animation-delay:0.1s">
    <div class="panel-head">
      <span class="panel-title">Data Input</span>
    </div>
    <div class="panel-body">
      <div class="tabs" style="margin-bottom:18px">
        <button class="tab-btn active" onclick="switchTab('image',this)">⬡ Upload image</button>
        <button class="tab-btn" onclick="switchTab('data',this)">⬡ Build chart</button>
        <button class="tab-btn" onclick="switchTab('text',this)">⬡ Describe data</button>
      </div>

      <!-- Image tab -->
      <div class="tab-content active" id="tab-image">
        <div class="upload-zone" id="uploadZone" onclick="document.getElementById('fileInput').click()" ondragover="dragOver(event)" ondragleave="dragLeave(event)" ondrop="dropFile(event)">
          <div class="upload-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="3" y="3" width="18" height="18" rx="2"/>
              <path d="M3 14.5l5-5 4 4 3.5-3.5L21 15"/>
              <circle cx="8" cy="8.5" r="1.5"/>
            </svg>
          </div>
          <div class="upload-title">Drop your chart here</div>
          <div class="upload-sub">PNG · JPG · WEBP · SVG — or click to browse</div>
        </div>
        <input type="file" id="fileInput" accept="image/*" style="display:none" onchange="handleFile(event)">
        <div class="img-preview-wrap" id="imgPreviewWrap" style="display:none">
          <img class="img-preview" id="imgPreview" />
          <button class="remove-btn" onclick="clearImage()">✕ Remove image</button>
        </div>
      </div>

      <!-- Data tab -->
      <div class="tab-content" id="tab-data">
        <div class="grid2">
          <div class="field-group">
            <div class="field-label">Chart type</div>
            <select class="field-select" id="chartType" onchange="renderPreviewChart()">
              <option value="bar">Bar chart</option>
              <option value="line">Line chart</option>
              <option value="pie">Pie chart</option>
              <option value="doughnut">Doughnut</option>
              <option value="radar">Radar</option>
            </select>
          </div>
          <div class="field-group">
            <div class="field-label">Chart title</div>
            <input class="field-input" id="chartTitle" placeholder="e.g. Monthly revenue" oninput="renderPreviewChart()" />
          </div>
        </div>
        <div class="field-group">
          <div class="field-label">Labels (comma-separated)</div>
          <input class="field-input" id="chartLabels" value="Jan, Feb, Mar, Apr, May, Jun" oninput="renderPreviewChart()" />
        </div>
        <div class="field-group">
          <div class="field-label">Values (comma-separated)</div>
          <input class="field-input" id="chartValues" value="42, 67, 55, 80, 73, 91" oninput="renderPreviewChart()" />
        </div>
        <div class="chart-preview-box">
          <canvas id="previewCanvas"></canvas>
        </div>
      </div>

      <!-- Text tab -->
      <div class="tab-content" id="tab-text">
        <div class="field-group">
          <div class="field-label">Paste data or describe your chart</div>
          <textarea class="field-textarea" id="textInput" placeholder="e.g. Bar chart — Monthly revenue 2024&#10;Jan: $42k · Feb: $67k · Mar: $55k · Apr: $80k · May: $73k · Jun: $91k&#10;&#10;The chart shows a general upward trend with a dip in March due to seasonal factors..."></textarea>
        </div>
      </div>
    </div>
  </div>

  <!-- Analysis type -->
  <div class="panel" style="animation-delay:0.15s">
    <div class="panel-head">
      <span class="panel-title">Analysis Focus</span>
    </div>
    <div class="panel-body">
      <div class="chips">
        <span class="chip" onclick="selectChip(this)">Entry Point &amp; TP</span>
        <span class="chip" onclick="selectChip(this)">Support &amp; Resistance</span>
        <span class="chip" onclick="selectChip(this)">Trend Direction</span>
        <span class="chip" onclick="selectChip(this)">Detect anomalies</span>
        <span class="chip" onclick="selectChip(this)">Risk &amp; Stop Loss</span>
        <span class="chip" onclick="selectChip(this)">Buy or Sell Signal</span>
        <span class="chip selected" onclick="selectChip(this)">Full V75 Analysis</span>
      </div>
      <input class="custom-input" id="customPrompt" value="Full V75 Analysis" placeholder="Or type a custom question..." />
    </div>
  </div>

  <!-- Error -->
  <div class="error-box" id="errorBox"></div>

  <!-- Button -->
  <button class="analyse-btn" id="analyseBtn" onclick="analyse()">
    <div class="btn-icon">
      <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>
      </svg>
    </div>
    Run V75 Signal Analysis
  </button>

  <!-- Result -->
  <div class="result-panel" id="resultPanel">
    <div class="result-head">
      <div class="result-head-left">
        <div class="result-dot"></div>
        <span class="result-title">Signal Output</span>
      </div>
      <span class="model-tag" id="modelTag">—</span>
    </div>
    <div class="result-body" id="resultBody"></div>
  </div>

  <div class="footer">
    Focus Fx Auto V75 TRADERS · Built with <a href="https://console.groq.com" target="_blank">Groq API</a> · Ultra-fast inference · Streaming responses
  </div>

</div>

<script>
  let currentTab = 'image';
  let previewChart = null;
  let uploadedB64 = null;
  let uploadedMime = 'image/jpeg';

  // Tab switching
  function switchTab(tab, btn) {
    currentTab = tab;
    document.querySelectorAll('.tab-content').forEach(el => el.classList.remove('active'));
    document.getElementById('tab-' + tab).classList.add('active');
    document.querySelectorAll('.tab-btn').forEach(el => el.classList.remove('active'));
    btn.classList.add('active');
    if (tab === 'data') renderPreviewChart();
  }

  // Chip selection
  function selectChip(el) {
    document.querySelectorAll('.chip').forEach(c => c.classList.remove('selected'));
    el.classList.add('selected');
    document.getElementById('customPrompt').value = el.textContent;
  }

  // Drag & drop
  function dragOver(e) { e.preventDefault(); document.getElementById('uploadZone').classList.add('drag'); }
  function dragLeave() { document.getElementById('uploadZone').classList.remove('drag'); }
  function dropFile(e) {
    e.preventDefault();
    dragLeave();
    const f = e.dataTransfer.files[0];
    if (f && f.type.startsWith('image/')) processFile(f);
  }
  function handleFile(e) { const f = e.target.files[0]; if (f) processFile(f); }

  function processFile(file) {
    uploadedMime = file.type || 'image/jpeg';
    const reader = new FileReader();
    reader.onload = ev => {
      uploadedB64 = ev.target.result.split(',')[1];
      document.getElementById('imgPreview').src = ev.target.result;
      document.getElementById('imgPreviewWrap').style.display = 'block';
      document.getElementById('uploadZone').style.display = 'none';
    };
    reader.readAsDataURL(file);
  }

  function clearImage() {
    uploadedB64 = null;
    document.getElementById('imgPreviewWrap').style.display = 'none';
    document.getElementById('uploadZone').style.display = 'flex';
    document.getElementById('fileInput').value = '';
  }

  // Preview chart
  function renderPreviewChart() {
    const labels = document.getElementById('chartLabels').value.split(',').map(s => s.trim());
    const values = document.getElementById('chartValues').value.split(',').map(s => parseFloat(s) || 0);
    const type = document.getElementById('chartType').value;
    const title = document.getElementById('chartTitle').value;
    const ctx = document.getElementById('previewCanvas').getContext('2d');
    if (previewChart) previewChart.destroy();

    const palette = ['#63d2a0','#5ba3e0','#f0a94a','#ff6b6b','#c084fc','#fb923c','#34d399'];
    const isCategorical = ['pie','doughnut'].includes(type);

    previewChart = new Chart(ctx, {
      type,
      data: {
        labels,
        datasets: [{
          label: title || 'Dataset',
          data: values,
          backgroundColor: isCategorical ? palette : 'rgba(99,210,160,0.6)',
          borderColor: isCategorical ? palette.map(c => c) : '#63d2a0',
          borderWidth: type === 'line' ? 2 : 1,
          tension: 0.35,
          fill: false,
          pointBackgroundColor: '#63d2a0',
          pointRadius: type === 'line' ? 4 : 0,
        }]
      },
      options: {
        responsive: true,
        plugins: {
          legend: { labels: { color: '#7a9d8a', font: { family: "'DM Mono', monospace", size: 11 } } },
          title: title ? { display: true, text: title, color: '#e8f0ea', font: { family: "'Syne', sans-serif", size: 13, weight: '600' } } : { display: false }
        },
        scales: ['pie','doughnut','radar'].includes(type) ? {} : {
          x: { grid: { color: 'rgba(99,210,160,0.06)' }, ticks: { color: '#7a9d8a', font: { family: "'DM Mono', monospace", size: 10 } } },
          y: { grid: { color: 'rgba(99,210,160,0.06)' }, ticks: { color: '#7a9d8a', font: { family: "'DM Mono', monospace", size: 10 } } }
        }
      }
    });
  }

  // Show/hide error
  function showError(msg) {
    const box = document.getElementById('errorBox');
    box.textContent = msg;
    box.style.display = msg ? 'block' : 'none';
  }

  // Main analysis
  async function analyse() {
    const apiKey = document.getElementById('apiKey').value.trim();
    const question = document.getElementById('customPrompt').value.trim() || 'Provide a full, comprehensive analysis of this chart.';
    showError('');

    if (!apiKey) { showError('// ERROR: Groq API key required. Get yours at console.groq.com'); return; }

    const btn = document.getElementById('analyseBtn');
    btn.disabled = true;
    btn.innerHTML = `<div class="typing"><span></span><span></span><span></span></div> Analysing...`;

    const panel = document.getElementById('resultPanel');
    const body = document.getElementById('resultBody');
    panel.style.display = 'block';
    body.innerHTML = '<div class="typing"><span></span><span></span><span></span></div>';
    panel.scrollIntoView({ behavior: 'smooth', block: 'nearest' });

    let model, messages;

    try {
      if (currentTab === 'image' && uploadedB64) {
        model = 'meta-llama/llama-4-scout-17b-16e-instruct';
        messages = [{
          role: 'user',
          content: [
            { type: 'image_url', image_url: { url: `data:${uploadedMime};base64,${uploadedB64}` } },
            { type: 'text', text: `You are an expert Forex and V75 (Volatility 75 Index) trading analyst. Analyse this chart carefully and respond to: ${question}\n\nStructure your response with clear sections using this format:\n\n▸ MARKET OVERVIEW\n[brief description of current price action and chart context]\n\n▸ TREND DIRECTION\n[bullish / bearish / ranging — with reasoning from price structure, EMAs, or indicators visible]\n\n▸ KEY LEVELS\n[major support and resistance levels with exact prices if visible]\n\n▸ ENTRY POINT\n[specific recommended entry price or zone, with reasoning — e.g. "Buy at 1.2345 on retest of support" or "Sell at 1.2500 breakout confirmation"]\n\n▸ TAKE PROFIT (TP)\n[TP1, TP2, and TP3 levels with price targets and R:R ratio if calculable]\n\n▸ STOP LOSS\n[recommended stop loss level with reasoning]\n\n▸ SIGNAL SUMMARY\n[BUY / SELL / WAIT — one clear verdict with confidence level]\n\nBe specific with price levels and percentages where visible.` }
          ]
        }];
      } else if (currentTab === 'data') {
        model = 'llama-3.3-70b-versatile';
        const labels = document.getElementById('chartLabels').value;
        const values = document.getElementById('chartValues').value;
        const cType = document.getElementById('chartType').value;
        const cTitle = document.getElementById('chartTitle').value;
        messages = [{
          role: 'user',
          content: `You are an expert Forex and V75 (Volatility 75 Index) trading analyst. Analyse the following chart data and respond to: ${question}\n\nChart type: ${cType}\nTitle: ${cTitle || 'Untitled'}\nLabels: ${labels}\nValues: ${values}\n\nStructure using:\n▸ MARKET OVERVIEW\n▸ TREND DIRECTION\n▸ KEY LEVELS\n▸ ENTRY POINT\n[specific entry price or zone with reasoning]\n▸ TAKE PROFIT (TP)\n[TP1, TP2, TP3 targets with R:R ratio]\n▸ STOP LOSS\n▸ SIGNAL SUMMARY\n[BUY / SELL / WAIT with confidence level]\n\nBe specific with price levels, percentages, and growth rates.`
        }];
      } else if (currentTab === 'text') {
        const text = document.getElementById('textInput').value.trim();
        if (!text) {
          showError('// ERROR: Please describe your chart or paste data first.');
          btn.disabled = false;
          btn.innerHTML = `<div class="btn-icon"><svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg></div> Run V75 Signal Analysis`;
          return;
        }
        model = 'llama-3.3-70b-versatile';
        messages = [{
          role: 'user',
          content: `You are an expert Forex and V75 (Volatility 75 Index) trading analyst. Based on this chart description, respond to: ${question}\n\nData:\n${text}\n\nStructure using:\n▸ MARKET OVERVIEW\n▸ TREND DIRECTION\n▸ KEY LEVELS\n▸ ENTRY POINT\n[specific entry price or zone with reasoning]\n▸ TAKE PROFIT (TP)\n[TP1, TP2, TP3 targets with R:R ratio]\n▸ STOP LOSS\n▸ SIGNAL SUMMARY\n[BUY / SELL / WAIT with confidence level]`
        }];
      } else {
        showError('// ERROR: No data input provided. Upload an image, build a chart, or describe your data.');
        btn.disabled = false;
        btn.innerHTML = `<div class="btn-icon"><svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg></div> Run V75 Signal Analysis`;
        return;
      }

      document.getElementById('modelTag').textContent = model.split('/').pop();

      const res = await fetch('https://api.groq.com/openai/v1/chat/completions', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Authorization': `Bearer ${apiKey}`
        },
        body: JSON.stringify({ model, messages, max_tokens: 1500, temperature: 0.4, stream: true })
      });

      if (!res.ok) {
        const err = await res.json();
        throw new Error(err.error?.message || `HTTP ${res.status}`);
      }

      body.textContent = '';
      const reader = res.body.getReader();
      const dec = new TextDecoder();
      let buf = '';

      while (true) {
        const { done, value } = await reader.read();
        if (done) break;
        buf += dec.decode(value, { stream: true });
        const lines = buf.split('\n');
        buf = lines.pop();
        for (const line of lines) {
          if (!line.startsWith('data: ')) continue;
          const d = line.slice(6).trim();
          if (d === '[DONE]') break;
          try {
            const j = JSON.parse(d);
            const delta = j.choices?.[0]?.delta?.content || '';
            body.textContent += delta;
          } catch {}
        }
      }

    } catch(e) {
      panel.style.display = 'none';
      showError(`// ERROR: ${e.message}`);
    }

    btn.disabled = false;
    btn.innerHTML = `<div class="btn-icon"><svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg></div> Run V75 Signal Analysis`;
  }

  // Init
  renderPreviewChart();
</script>
</body>
</html>
